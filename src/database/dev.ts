import { configDBSequelize } from "./keys";

const configDev:configDBSequelize = {
    user: "root",
    password:"root",
    db: "xplcd_shop_local",
    host: "localhost"
}

export default configDev;