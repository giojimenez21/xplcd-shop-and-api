import { LoginContainer } from "../login"
import { RegisterForm } from "./components"

const Register = () => {
    return (
        <LoginContainer>
            <RegisterForm />
        </LoginContainer>
    )
}

export default Register;